name: AP1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter


  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_gherkin: ^2.0.0
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  firebase_storage: ^11.6.9
  cloud_firestore: ^4.15.8

dev_dependencies:
  flutter_driver:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
  assets:
  - ProductsImages/assets/tshirt_LOGO.png

  fonts:
    - family: GloriaFont
      fonts:
        - asset: ProductsImages/font/GloriaHallelujah-Regular.ttf

  uses-material-design: true

